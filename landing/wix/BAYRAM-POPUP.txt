<!-- =====================================================
     Wix: Ayarlar → Özel Kod (Custom Code) → Sayfa Sonu (Body End)
     "Tüm sayfalar" seç → Kod Ekle → Aşağıdakini yapıştır → Uygula

     ÖNCE GÖRSELİ WIX MEDIA'YA YÜKLE:
       1) Wix Editör → sol menü → Medya (Media)
       2) "landing/icons/bayram-2026.png" dosyasını yükle
       3) Yükledikten sonra dosyaya sağ tıkla → "Tüm Detaylar" / "Public URL'i kopyala"
          (URL şu şekilde olur: https://static.wixstatic.com/media/...png)
       4) Aşağıdaki BAYRAM_IMG_URL yerine o URL'i yapıştır.
     ===================================================== -->

<style id="bilye-bayram-style">
  #bilye-bayram-popup {
    position: fixed;
    inset: 0;
    z-index: 2147483000;
    display: none;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }
  #bilye-bayram-popup.is-open { display: flex; }
  #bilye-bayram-popup .bb-backdrop {
    position: absolute; inset: 0;
    background: rgba(6,8,15,0.82);
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
    animation: bb-fade 0.32s ease;
    cursor: pointer;
  }
  #bilye-bayram-popup .bb-card {
    position: relative; z-index: 1;
    width: 100%;
    max-width: 420px;
    max-height: calc(100vh - 40px);
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 30px 60px rgba(0,0,0,0.6);
    animation: bb-pop 0.4s cubic-bezier(0.34,1.56,0.64,1) both;
    background: #0f172a;
    line-height: 0;
  }
  #bilye-bayram-popup .bb-img {
    display: block;
    width: 100%;
    height: auto;
    max-height: calc(100vh - 40px);
    object-fit: contain;
  }
  #bilye-bayram-popup .bb-close {
    position: absolute;
    top: 10px; right: 10px;
    width: 36px; height: 36px;
    border-radius: 50%;
    border: none;
    background: rgba(6,8,15,0.7);
    color: #fff;
    font-size: 22px; line-height: 1;
    cursor: pointer;
    display: grid; place-items: center;
    z-index: 2;
    transition: background 0.15s ease, transform 0.15s ease;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }
  #bilye-bayram-popup .bb-close:hover {
    background: rgba(6,8,15,0.95);
    transform: scale(1.05);
  }
  body.bilye-bayram-open { overflow: hidden; }
  @keyframes bb-fade { from { opacity: 0; } to { opacity: 1; } }
  @keyframes bb-pop {
    0% { transform: translateY(20px) scale(0.96); opacity: 0; }
    100% { transform: translateY(0) scale(1); opacity: 1; }
  }
</style>

<div id="bilye-bayram-popup" role="dialog" aria-modal="true" aria-label="Bayram kutlaması" aria-hidden="true">
  <div class="bb-backdrop" data-bb-close></div>
  <div class="bb-card">
    <button type="button" class="bb-close" data-bb-close aria-label="Kapat">×</button>
    <img class="bb-img" src="https://i.ibb.co/Kz3zvbhg/bayram-2026.jpg" alt="Bayramınız kutlu olsun">
  </div>
</div>

<script>
  (function () {
    var KEY = "bilye-bayram-2026";
    var popup = document.getElementById("bilye-bayram-popup");
    if (!popup) return;
    try { if (localStorage.getItem(KEY) === "1") return; } catch (e) {}

    function open() {
      popup.classList.add("is-open");
      popup.setAttribute("aria-hidden", "false");
      document.body.classList.add("bilye-bayram-open");
    }
    function close() {
      popup.classList.remove("is-open");
      popup.setAttribute("aria-hidden", "true");
      document.body.classList.remove("bilye-bayram-open");
      try { localStorage.setItem(KEY, "1"); } catch (e) {}
    }
    popup.querySelectorAll("[data-bb-close]").forEach(function (el) {
      el.addEventListener("click", close);
    });
    document.addEventListener("keydown", function (e) {
      if (e.key === "Escape" && popup.classList.contains("is-open")) close();
    });
    function start() { setTimeout(open, 600); }
    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", start);
    } else {
      start();
    }
  })();
</script>
